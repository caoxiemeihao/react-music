.player-song-name {
  position: relative;
	padding: 20px;
	color: #fff;
	text-align: center;
  
  .back {
    position: absolute;
    width: 30px;
    height: 30px;
    background-size: 100%;
    background-repeat: no-repeat;
  }

	.name {
		font-size: 16px;
		padding-bottom: 8px;
	}

	.singer {
		font-size: 12px;
		color: rgba(255, 255, 255, 0.6);
	}
}
